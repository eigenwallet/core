---
description: Don't run cargo commands. Ask the user to compile.
globs:
alwaysApply: true
---

Only run cargo commands after asking the user. Prefer not to run cargo commands. Ask the user to compile.

If the user says "run cargo commands" then use cargo commands.
